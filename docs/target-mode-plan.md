# 目标模式计划

> 执行期目标源。详细过程保留在 Git 历史；本文件只保留当前约束、冻结协议、验证基线、活跃队列和少量追溯锚点。

## 目标与边界

- PPT 插件作为科研绘图中枢：Rough 原生图形、SimpleExperiment 自动绘图、Zotero 论文图像与配色库。
- Rough 原生图形和 SimpleExperiment 自动绘图最终必须是 PPT 原生可编辑对象；禁止 PNG、SVG、Canvas 截图作为这些链路的最终对象。
- Zotero 论文参考图像与白名单科研绘图网站导出的受校验 SVG 是相互隔离的外部图像例外；科研 SVG 例外不得扩展到 Rough 或自动绘图。
- 所有界面使用中文母语表达；必要英文只保留官方名称或缩写，并与中文语义组合。意义不明确的按钮、标题、徽标和命令必须有中文悬浮说明。
- Ribbon 放高频入口；右侧任务窗格保留完整参数、素材与配色管理和兜底操作。
- 优先优化 UI 外观、布局、发现性、首屏密度、图标一致性、状态可读性和功能边界；阻塞、危险操作或兼容断裂可插队。
- 插件整体保留“插入 / 重绘 / 风格 / 数据”主路径；插入和重绘由 Ribbon 执行，右侧简洁模式只显示当前选区状态或单个参数/数据工作区，完整模式保留全部专业面板。
- 右侧任务窗格命令和导航使用纯文字；仅原生形状预览、素材/论文图缩略图、配色色块和状态标记保留视觉内容。
- 未明确要求时不安装、不部署、不关闭或重启 PowerPoint/VS Code、不打包。

## 执行规则

- 每批开始先读 `docs/PROJECT_CONSTRAINTS.md`、本文件、`git status --short --branch` 和最近提交。
- 脏文件先判定归属；禁止覆盖、回滚、暂存或提交用户改动。
- 涉及外部兼容时，只读核对 `D:\GitRepo\MCP\zlk-cluster-orchestrator`；每个实现批次开始前必须重新读取 `D:\GitRepo\my_img_manager` 的状态、最近提交和相关协议。禁止修改外部仓或追随未冻结协议。
- 新故障先写入本计划或验证清单，再修代码；修复后同步验证相邻旧合同。
- 每个小批次只做一个同风险面改动并独立提交；单个小批次不运行测试、构建或打包。
- 每完成连续 5 个小批次，统一运行完整 `npm test` 和 UI 构建；涉及 C# 时运行 Release `scripts/build.ps1`。全部通过后再按用户要求打包。
- 五批次统一验证失败时先修复并重新完整验证；禁止产出或交付已知失败的安装包。
- 人工清理候选仅限需要完整删除的文件或目录；保留文件内的局部内容由 Codex 直接修改，但禁止清空整个文件。
- UI 批次必须保持中文、tooltip、危险操作不直接执行，以及非顶栏内容不使用 sticky/fixed 遮挡滚动。
- 修改 `src/RoughPptAddin/ui/**` 后，在五批次节点运行 `npm run build:ui`。
- 提交信息只描述已验证能力，不写未验证承诺。
- 禁止启用多角色或多智能体后台流程。

## 冻结协议

### SimpleExperiment / ZLK Cluster

- discovery：`%LOCALAPPDATA%\RoughPptAddin\automation.json`、`automation.token`。
- endpoint：`GET /health`、`POST /api/simple-experiment/plot`，并保留旧兼容端点 `POST /api/zlk-cluster/plot`；仅允许 `127.0.0.1` / `localhost`。
- token header：`Authorization: Bearer`、`X-Rough-Ppt-Token`、`X-RoughPpt-Automation-Token`。
- `schemaVersion=1`；未知字段忽略，未来只接受 optional additive extensions。
- target 已打开则复用追加；存在未打开则打开追加；不存在且允许时新建保存；不得关闭已有 PPT 或退出 PowerPoint。
- 结果必须经 `zlk-cluster-result-importer.mjs` 归一化，再用 PPT 原生 shape、line、text、table 绘制。
- 默认优先当前 `simple_cluster/results/statistics.json` 和论文表格 CSV；旧项目兼容 `zlk_cluster/`。单 seed/raw result 仅用于发现、追踪和审计。
- Markdown 摘要只生成原生文本表格摘要页；存在同名 JSON 时优先 JSON。
- 相同 `requestId` 可安全重放；不同内容必须返回中文冲突。忙碌时快速返回，不进入 UI 队列。

### Zotero Image Saver

- 完整协议源：`docs/ZOTERO_EXTERNAL_DATABASE_PROTOCOL.md`；PPT 只读外部 SQLite，禁止读取 `zotero.sqlite`。
- 唯一数据库固定为 `%LOCALAPPDATA%\ZLK\paper-image-library\paper_images.sqlite`；其它路径均回落固定路径。
- 表优先级：`images` -> `paper_images` -> 兼容候选表；只读活动行，并读取 palette 字段。
- bridge 只允许状态、刷新、打开 PDF 和定位条目/附件命令；不可用时使用受控 fallback。
- 完整图库界面由 Zotero 唯一维护；PPT 不复制第二套图库，也不发送删除、分享或导入命令。
- 返回非法 Zotero URI 时禁止 ShellExecute，只复制溯源 ID。

## 验证基线

- 轻量：`node scripts/validate-ui-contract.mjs`、相关专项合同脚本、`node --check src/RoughPptAddin/ui/app.mjs`。
- 外部协议：自动化合同、Zotero 图像库合同和外部插件兼容验证。
- 全量：`npm test`。
- 构建：`npm run build:ui`；C# Release 构建为 `powershell -ExecutionPolicy Bypass -File scripts/build.ps1`。
- 打包：仅用户明确要求时运行 `npm run package`；只打包，不安装。
- 提交前：`git diff --check`、`git status --short`。

## 功能归属

| 区域 | 功能类型 | 当前功能 |
| --- | --- | --- |
| Ribbon | 高频直接执行 | 形状图库、选区动作、风格快捷项、特征块、论文套件、保存素材 |
| 右侧任务窗格 | 参数配置、预览、资源管理 | 风格精调、科研绘图、论文预设、Zotero 图像与配色、特征块方向、素材管理 |
| 更新入口 | 手动检查 | 固定 GitHub Release API 查询；新版本只引导用户手动下载安装 |

右侧任务窗格不得再次显示已经由 Ribbon 直接执行的高频命令。保留隐藏接线仅用于兼容与回归测试，不构成可见入口。

## 活跃队列

1. B596-B600（已完成）：新增 GitHub 正式 Release 检查入口、文档、截图、合同测试和桥接注册；统一测试、UI 构建和 Release 构建通过。
2. B601-B605（已完成）：横向风格控件支持拖动/键盘滚动，加宽模板卡，隐藏收起态状态滚动器，补齐网络失败提示，重拍总览/风格/特征说明截图；统一测试、UI 构建和 Release 构建通过。
3. B606（已完成）：限制快捷工作台最大宽度，避免宽任务窗格下主路径按钮被拉伸成超宽控件。
4. B607（已完成）：常用搜索建议横向列表纳入统一拖动/键盘滚动交互，避免窄窗下建议按钮溢出不可达。
5. B608（已完成）：常用功能命令横向命令中心纳入统一拖动/键盘滚动交互，窄窗下不再出现不可达的溢出按钮。
6. B609（已完成）：限制折叠工作流、形状图库开关和快速插入工具的宽窗最大宽度，避免次要控件在宽面板中拉伸成巨幅按钮。
7. B610（已完成）：限制顶栏版本胶囊宽度，避免版本检测按钮在宽窗或中等窗格中拉伸成整行控件。
8. B606-B610（已完成统一验证）：完整测试、UI 构建和 Release 构建全部通过。
9. B611（已完成）：外部 Zotero 工作区恢复视图版本 37 后，跨仓合同恢复一致；本地构建元数据已随 Release 构建更新。
10. B612（已完成）：压缩简洁模式主路径卡片视觉密度，保持 320 到 1000px 两列可读布局。
11. B613（已完成）：消除窄栏长状态的内部滚动箭头，让常见错误提示在折叠态完整可读。
12. B614（已完成）：把“论文预设 AI”改为语义更自然的“AI 论文预设”，同步中文发现性合同。
13. B615（已完成）：修复 Zotero 搜索框被全局紧凑覆盖改为单列导致的图标和输入框纵向堆叠；任务窗格、科研绘图和形状图库已复核。
14. B616（已完成）：UI 验证浏览器改用独立临时配置目录，并优先使用本机 Chrome；同步启动状态合同到当前中文提示。B611-B616 完整测试、UI 构建和 Release 构建通过。
15. B617（已完成）：隐藏空数量徽标并压缩折叠面板高度；空状态密度检查通过。
16. B618（已完成）：任务窗格与形状图库搜索框统一为 32px 单行、8px 圆角、无边框内嵌输入和 18px 图标。
17. B619（已完成）：简洁/完整模式使用高对比选中态；窄栏顶栏压缩到 114px，隐藏次要品牌副标题。
18. B620（已完成）：复核确认科研绘图打开时已用内置示例自动渲染同源 SVG 预览；无需改动。
19. B621（已完成）：发布链路改用标准构建加 Mage 生成/签名平面 VSTO 清单；证书查询、哈希计算和安装器信任写入不再依赖受损的 PowerShell 证书提供方。ZIP、MSI 和 EXE 生成成功，MSI 已无感安装。
20. B622（已完成）：快速插入移除按钮改为悬停或键盘聚焦时显示，避免红色圆形控件持续遮挡形状预览。
21. B623（已完成）：用包含 B622 的源码重新生成 0.1.877 三格式安装包，并通过 MSI 无感安装；安装载荷与发布包关键文件哈希一致。
22. B624（已完成）：快速插入图标改为立即渲染并关闭局部懒渲染；完整模式整页截图已确认六个图标可见。
23. B625（已完成）：UI 构建和同步统一写入安装器产品版本；简洁模式版本按钮改为显示产品版本，不再只显示提交号。
24. B626（已完成）：用包含 B624/B625 的源码生成 0.1.880 三格式安装包，并通过 MSI 无感安装；安装载荷与发布包一致。
25. B627（已完成）：科研绘图宽窗改为三栏独立滚动，预览按可用宽高等比居中，消除整页滚动和长片空白。
26. B628（已完成）：新增帮助截图重建脚本和 npm 入口，重建任务窗格、风格、特征块和科研绘图帮助截图，使离线说明与当前 UI 一致。

## 近期锚点

- B596：更新检查读取安装包 build-info，只访问固定 GitHub API/Releases 地址，拒绝草稿与预发布，限制响应体积，明确“关闭 PowerPoint 后手动安装”。
- B597-B598：README、Markdown 使用说明、离线帮助和更新链路静态合同必须同步入口、结果动作、网络边界和手动安装步骤。
- B599-B600：说明截图随界面变化重拍；前端新增宿主消息必须同步 `bridge-contract.mjs`。
- B601：横向内容提供拖动、方向键、中文 aria-label 和 tooltip；模板卡预留足够宽度；收起态状态条隐藏滚动器但保留换行。
- B602：空响应、GitHub 限流和连接失败转换为中文可操作提示，不暴露原始异常细节。
- B603-B604：总览、风格和特征截图使用绿色收起状态、真实上下文徽标、当前顶栏和控件布局。
- B601-B605 统一验证：`npm.cmd test`、`npm.cmd run build:ui` 和 Release `scripts/build.ps1` 全绿；产物仅暂存，未打包安装。
- B606：快捷工作台在 320–1000px 内保持两列、横排文本和可读尺寸；按钮宽度不得超过约 340px，避免宽窗拉伸。
- B607：所有横向建议/预设轨道必须有中文 aria-label、tooltip、鼠标拖动、键盘滚动和静态合同；不得只依赖原生滚动条。
- B608：命令中心在窄窗保持 128px 可读卡片；溢出时必须可拖动、可用方向键滚动，并纳入交互合同。
- B609：宽窗下的低频/辅助控件必须保留可点击面积但不占满整行；折叠按钮约 340px 内，图库入口约 520px 内，快速插入工具约 320px 内。
- B610：版本胶囊只承载短版本信息，最大约 240px；长状态仍由独立状态条展开，不得让版本按钮替代长文区域。
- B606-B610 统一验证：`npm.cmd test`、`npm.cmd run build:ui` 和 Release `scripts/build.ps1` 全绿；压缩计划时补回 Zotero 批次前外部仓复核和 B116 合同锚点。产物仅暂存，未打包安装。
- B611 观察基线：当前源码已静默安装并核对 `publish/ui` 与 `%LOCALAPPDATA%\RoughPptAddin\publish/ui` 哈希一致；420px 截图显示本地构建元数据停留在旧提交，且长状态出现内部滚动箭头。
- B612-B614：主路径卡片收敛到 560px、36px 高、8px 圆角和中性底色；折叠状态允许常见三行提示完整显示；入口改为“AI 论文预设”。`node --check`、UI 合同、简洁布局、任务窗格交互和编码检查通过；420px 截图复核后已回收。
- B615 观察基线：当前源码已无感安装，420px 截图显示 Zotero 搜索图标和输入框纵向堆叠，搜索区高度膨胀；需恢复图标与输入框同一行并复核相关窗口。
- B615 验证锚点：Zotero 搜索框恢复 30px 单行布局；`node --check`、UI 资产合同、UI 合同和外部兼容合同通过；已安装任务窗格、科研绘图示例渲染和形状图库截图复核后回收。完整统一验证先停在 Edge 启动器进程模型，由 B616 处理。
- B616 统一验证：`npm test`、`npm run build:ui` 和 Release `scripts/build.ps1` 全绿；最新载荷已无感安装。UI 验证浏览器不再复用仓库内浏览器配置，启动状态合同改为检查宿主连接和目录降级合并提示。
- B617 验证锚点：空徽标不再占位，折叠论文预设和素材面板高度降到 37px；UI 资产与 UI 合同通过，完整模式截图复核后回收。截图另发现长状态粘性顶栏可遮挡形状首行，转入 B619。
- B619 验证锚点：420px 简洁/完整模式切换后顶栏均保持 114px，选中按钮为白字蓝底；UI 资产与 UI 合同通过，简洁模式截图复核后回收。粘性遮挡复判为 Playwright scrollIntoViewIfNeeded 忽略 scroll-margin，不改变原生滚动合同。
- B618 验证锚点：主搜索、Zotero 搜索和形状图库搜索均为 32px 外框、28px 透明输入、同层单行；UI 资产与 UI 合同通过，Zotero 搜索截图复核后回收。
- B620 验证锚点：HTTP 本地运行时下内置示例自动生成 894x619 SVG，图片加载完成且预览可见；保留手动刷新和宿主校验链路。
- B621 验证锚点：`npm run package` 全绿并生成 r10 三格式安装包；MSI 静默安装返回 0，安装日志显示 VSTO 注册完成，`LoadBehavior=3`，发布包与安装载荷关键文件哈希一致，VSTO 部署清单 Mage 验证有效。
- B622 验证锚点：快速插入移除按钮默认 `opacity:0` 且不接收指针，悬停后恢复可见；UI 资产与 UI 合同通过，静置截图只显示黑色形状预览并已回收。
- B623 验证锚点：`releases\RoughPptAddin-0.1.877-a8ab4ea8` 生成 ZIP、MSI 和 EXE；MSI 静默返回 0，安装元数据为 `0.1.877 / a8ab4ea8`，`LoadBehavior=3`，DLL、VSTO、清单和 UI 哈希一致。
- B624 验证锚点：六个快速插入 canvas 的 `previewDrawn=1` 且非透明像素均超过 20；UI 资产、UI 合同、形状图库合同和 `node --check` 通过，整页截图确认图标可见后已回收。
- B625 验证锚点：独立构建与同步后 `build-info.version=0.1.879`；320px 简洁模式按钮显示 `版本 v0.1.879*`。UI 资产、UI 合同、更新检查和 `node --check` 通过，最新 UI 已同步到安装载荷。
- B626 验证锚点：`npm test`、UI 构建和 `npm run package` 全绿；`releases\RoughPptAddin-0.1.880-a3aa4f14` 生成 ZIP、MSI 和 EXE。MSI 静默返回 0，安装元数据为 `0.1.880 / a3aa4f14`，`LoadBehavior=3`，关键载荷哈希一致。
- B627 验证锚点：1180px 默认窗口整页额外滚动为 0；预览图 498×345 在 528×661 容器内居中。UI 窗口布局、科研绘图运行时和键盘导航合同通过，截图复核后已回收。
- B628 观察基线：四张帮助截图仍显示旧提交、旧主路径布局和旧科研绘图长空白；README、USER_GUIDE 与内置帮助引用它们，需要随当前界面更新。
- B628 验证锚点：`npm test` 通过；四张帮助截图已按 2x 缩放重建并人工复核。总览、风格和特征截图显示 0.1.882 当前布局，科研绘图截图显示三栏独立滚动与居中 SVG 预览。

## 关键里程碑

- B596-B605：插件具备公开版文档、手动 GitHub Release 更新检查和更一致的横向控制/状态呈现。
- B525-B555：科研绘图扩展到 36 种图表并建立真实 Vega 运行时回归。
- B467-B474：Ribbon 收敛为五组唯一入口，建立当前用户覆盖安装和定向 ICE 抑制。
- B452-B466：离线图文说明、独立非模态窗口和 PowerPoint 2013 以上兼容合同建立。
- B116：Zotero 图像库读取遵守冻结的外部 SQLite 与受限 bridge 协议。
