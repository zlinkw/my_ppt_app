# my_ppt_app 恢复审计报告

更新时间：2026-07-21 14:10 +08:00

## 结论

审计脚本主体已恢复。本轮把明显过短的 `validate-ui-contract.mjs` 替换为 2026-07-13 Claude 文件历史中的 39293 字节精确备份。低层恢复目录没有 `my_ppt_app` 根索引，因此无法依据 TSK 列出删除前最终缺失文件。

## 已恢复与验证

- `scripts\validate-ui-contract.mjs`：8820 字节替换为 39293 字节精确历史版本，`node --check` 通过。
- 旧文件备份到 `__损坏文件副本__\GitRepo\my_ppt_app\scripts\validate-ui-contract.before_audit_recovery_20260721_135434_096.mjs`。
- 本轮检查 45 个 JS/MJS 文件，语法错误为 0。
- 全部 PowerShell 文件按 UTF-8 解码后语法错误为 0。
- 先前从 DLL/PDB 反编译并合并 29 个 C# 文件，Roslyn 验证为 0 错误、9 警告。
- `package-run.ps1` 已从 Codex 会话精确恢复，PowerShell 解析错误为 0；未执行。

## 已知问题

- TSK 的 `GitRepo` 索引没有 `my_ppt_app` 目录，无法统计最终版本的缺失路径和最终文件大小。
- `validate-ui-contract.mjs` 是 2026-07-13 的精确备份，不保证包含 7 月 19 日前的后续修改。
- 存在恢复抽取副产物：`package.json)`、`scripts\verify-deploy-package.ps1),`、`scripts\verify-native-all.ps1),`。按要求未删除。
- 反编译 C# 与原始源码可能存在变量名、注释、条件编译和格式差异。

## 潜在问题

- 验证脚本语法可用不代表其引用的 UI 资源、安装包、Office COM 环境和外部插件都完整。
- 当前项目由发布包、反编译源码、文件历史和会话文本混合恢复，文件时间点可能不一致。